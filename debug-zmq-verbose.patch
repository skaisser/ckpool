diff --git a/src/stratifier.c b/src/stratifier.c
index 1234567..abcdefg 100644
--- a/src/stratifier.c
+++ b/src/stratifier.c
@@ -8503,10 +8503,17 @@ static void *zmqnotify(void *arg)
 	
 	/* Main polling loop */
 	while (42) {
+		static time_t last_log = 0;
+		time_t now = time(NULL);
+		
+		if (now - last_log > 60) {
+			LOGDEBUG("ZMQ poll loop still running, monitoring %d endpoints", num_endpoints);
+			last_log = now;
+		}
+		
 		rc = zmq_poll(poll_items, num_endpoints, -1);  /* Block indefinitely */
 		
 		if (rc < 0) {
 			LOGWARNING("zmq_poll failed with error %d", errno);
 			sleep(1);
 			continue;
 		}
+		
+		LOGDEBUG("zmq_poll returned %d events", rc);
 		
 		/* Check which socket(s) have data */
 		for (i = 0; i < num_endpoints; i++) {