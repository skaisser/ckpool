*.o
*.bin
*.la
*.lo

autom4te.cache
.deps

Makefile
Makefile.in
INSTALL
aclocal.m4
configure
depcomp
missing
install-sh
stamp-h1
compile
config.log
config.status
config.guess
config.sub

*~

ext_deps
config.h.in
config.h

mkinstalldirs

*.swp
src/ckpool
src/ckpmsg
ltmain.sh

*.m4

.libs/

libtool

# macOS
.DS_Store

# Local planning dossiers (kept on disk, not published)
.planning/

# Go log API build output -- binaries are built on the build host and copied
# to the pool server, never committed (see api/README.md). There is no CI
# workflow for api/; verification runs on the build host.
api/ckpool-api
api/ckpool-api-linux-amd64
api/ckpool-api-linux-arm64
