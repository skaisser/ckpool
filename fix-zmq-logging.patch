diff --git a/src/stratifier.c b/src/stratifier.c
index 1234567..abcdefg 100644
--- a/src/stratifier.c
+++ b/src/stratifier.c
@@ -2150,6 +2150,7 @@ static void update_base(sdata_t *sdata, const workbase_t *wb, const bool clean)
 
 static void update_base(sdata_t *sdata, const int prio)
 {
+	LOGNOTICE("update_base called with priority %d", prio);
 	workbase_t *wb_base, *wb_orphan;
 	json_t *val, *json_orphan;
 	char header[228];
@@ -8542,7 +8543,7 @@ static void *zmqnotify(void *arg)
 						case 32:
 							update_base(sdata, GEN_PRIORITY);
 							__bin2hex(hexhash, zmq_msg_data(&message), 32);
-							LOGNOTICE("ZMQ block hash %s from endpoint %d", hexhash, i);
+							LOGWARNING("ZMQ: New block detected! Hash %s from endpoint %d", hexhash, i);
 							break;
 						default:
 							LOGWARNING("ZMQ message size error from endpoint %d, size = %d", 